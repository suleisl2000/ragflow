ragflow:
  host: 0.0.0.0
  http_port: 9380

admin:
  host: 0.0.0.0
  http_port: 9381

mysql:
  name: 'rag_flow'
  user: 'ragflow'
  password: 'ragflow_password_2024'
  host: 'localhost'
  port: 3306
  max_connections: 900
  stale_timeout: 300
  max_allowed_packet: 1073741824

minio:
  user: 'minioadmin'
  password: 'minioadmin'
  host: '127.0.0.1:9000'

infinity:
  uri: '127.0.0.1:23817'
  db_name: 'default_db'

redis:
  db: 1
  password: 'infini_rag_flow'
  host: 'localhost:6379'

ocr:
  # PaddleOCR 配置（默认使用）
  paddleocr:
    api_url: 'http://localhost:8080/layout-parsing'
  # Textin OCR 配置（备选）
  textin:
    app_id: ''
    secret_code: ''
    api_url: 'https://api.textin.com/ai/service/v1/pdf_to_markdown'

# Custom Parser 缓存bucket配置（支持环境变量覆盖）
# 环境变量优先级：PDF_CACHE_BUCKET, PADDLEOCR_CACHE_BUCKET, TEXTIN_CACHE_BUCKET
pdf_cache_bucket: 'medguide-pdf-cache'
paddleocr_cache_bucket: 'medguide-paddleocr-cache'
textin_cache_bucket: 'medguide-textin-cache'

user_default_llm:
  factory: 'LocalAI'  # 默认工厂
  api_key: 'empty'  # LocalAI 一般不需要真实的 API key
  base_url: ''  # 每个模型有自己的 base_url
  default_models:
    chat_model:
      name: 'qwen3-8b-fp8'
      factory: 'LocalAI'
      api_key: 'empty'
      base_url: 'http://localhost:8090'
    embedding_model:
      name: 'qwen3-embedding-8b'
      factory: 'LocalAI'
      api_key: 'empty'
      base_url: 'http://localhost:8082'

